/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

/** Shown when logout() returns false (arcade cleanup OR network/server sign-out failure). */
export const LOGOUT_ARCADE_BLOCKED =
  'Sign-out failed — leave any arcade queue/match, check your connection, then try again (session may still be active).';